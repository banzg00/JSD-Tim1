import { Component, OnInit } from '@angular/core';
import { {{entity.name}}Service } from './{{entity.name|lower}}.service'; 
import { ActivatedRoute } from '@angular/router';

@Component({
  selector: 'app-{{entity_name_dash}}-page',
  templateUrl: './{{entity_name_dash}}-page.component.html',
  styleUrls: ['./{{entity_name_dash}}-page.component.scss'],
})
export class {{entity.name}}Component {
  id: string = '';
  obj: any = null;

  constructor(private route: ActivatedRoute, private {{entity.name|lower}}Service: {{entity.name}}Service) { }

  ngOnInit(): void {
    this.route.paramMap.subscribe(params => {
      this.id = params.get('id') ?? '';
      this.{{entity.name|lower}}Service.get{{entity.name}}ById(this.id).subscribe((obj:any) => {
        this.obj = obj;
      });
    });
  }
}
